import { Box, Text } from '@/src/theme';

export default function UserScreen() {
  return (
    <Box flex={1} backgroundColor="mainBackground" justifyContent="center" alignItems="center">
      <Text variant="header">Profil utilisateur</Text>
    </Box>
  );
}
