import { Box, Text } from '@/src/theme';

export default function HistoriqueFormations() {
  return (
    <Box flex={1} backgroundColor="mainBackground" padding="l">
      <Text variant="header">Historique des formations</Text>
    </Box>
  );
}
