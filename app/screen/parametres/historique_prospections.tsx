import { Box, Text } from '@/src/theme';

export default function HistoriqueProspections() {
  return (
    <Box flex={1} backgroundColor="mainBackground" padding="l">
      <Text variant="header">Historique des prospections</Text>
    </Box>
  );
}
